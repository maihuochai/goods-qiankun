import Vue from 'vue'
import Vuex from 'vuex'
import globalData from '@/store/modules/globalData'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    globalData
  }
})
